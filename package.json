{
  "name": "tdd-basic-javascript",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "test": "jest --watchAll --verbose"
  },
  "devDependencies": {
    "vite": "^2.6.4"
  },
  "dependencies": {
    "jest": "^27.2.5"
  }
}
